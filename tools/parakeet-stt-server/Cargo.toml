[package]
name = "meeting-parakeet-stt-server"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1"
tempfile = "3"
tiny_http = "0.12"
transcribe-rs = { version = "0.3.11", features = ["onnx"] }
